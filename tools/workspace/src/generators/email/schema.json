{
  "$schema": "https://json-schema.org/schema",
  "$id": "Email",
  "title": "Generate Email Library",
  "description": "Generates an email library that uses react-email and resend for email sending",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use?"
    }
  },
  "required": []
}

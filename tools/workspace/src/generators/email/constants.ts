/**
 * Default name of the library.
 */
export const defaultLibName = 'email';

/**
 * Default directory of the library.
 */
export const defaultLibDirectory = '';

/**
 * Default tags of the library.
 */
export const defaultLibTags = ['server', 'email'];

/**
 * Dependencies of the library.
 */
export const deps = {
  '@react-email/components': '^0.0.27',
  '@react-email/tailwind': '^1.0.1',
} as const;

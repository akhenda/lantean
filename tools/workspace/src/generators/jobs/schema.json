{
  "$schema": "https://json-schema.org/schema",
  "$id": "Jobs",
  "title": "Generate Jobs Library",
  "description": "Generates a jobs library that uses trigger.dev",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use?"
    }
  },
  "required": ["name"]
}

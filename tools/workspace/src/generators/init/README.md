# @lantean/workspace:init

Setup command:

```shell
nx g @lantean/workspace:init
```

Configures [`lint-staged`](https://github.com/okonet/lint-staged) and [`husky`](https://github.com/typicode/husky) to lint files when being committed to the repo.

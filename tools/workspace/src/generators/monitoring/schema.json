{
  "$schema": "https://json-schema.org/schema",
  "$id": "Monitoring",
  "title": "Configure Monitoring",
  "description": "Generates a monitoring library that uses Sentry for monitoring",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use?"
    },
    "typesLibName": {
      "type": "string",
      "description": "The types library to use",
      "$default": {
        "$source": "projectName"
      },
      "x-prompt": "What name would you like to use for the types lib?"
    }
  },
  "required": []
}

/**
 * Default name of the library.
 */
export const defaultLibName = 'date';

/**
 * Default directory of the library.
 */
export const defaultLibDirectory = '';

/**
 * Default tags of the library.
 */
export const defaultLibTags = ['web', 'mobile', 'server', 'client', 'date'];

/**
 * Dependencies of the library.
 */
export const deps = {
  'dayjs': '^1.11.13',
} as const;

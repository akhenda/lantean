{
  "$schema": "https://json-schema.org/schema",
  "$id": "Env",
  "title": "Configures ENV",
  "description": "Configures Client & Server Environment Variables",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "",
      "default": "env",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the env lib?"
    }
  },
  "required": ["name"]
}
